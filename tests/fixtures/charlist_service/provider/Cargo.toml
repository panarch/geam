[package]
name = "geam-charlist-service-fixture"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
geam = { path = "../../../..", default-features = false, features = ["provider", "gleam-erlang"] }

[dev-dependencies]
camino = "1"

[workspace]
resolver = "3"
