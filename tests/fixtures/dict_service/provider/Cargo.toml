[package]
name = "geam-dict-service-fixture"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
geam = { path = "../../../..", default-features = false, features = ["provider", "gleam-stdlib"] }

[dev-dependencies]
camino = "1"

[workspace]
resolver = "3"
