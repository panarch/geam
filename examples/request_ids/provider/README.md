# geam-example-request-ids

Macro-authored Rust provider for the `example_request_ids` Gleam package. See
the [complete example](../README.md) for the default-state component, shared
and mutable state access, and standalone commands.
