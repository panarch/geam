# geam-example-feature-flags

Macro-authored Rust provider for the `example_feature_flags` Gleam package. See
the [complete example](../README.md) for explicit configuration, exact
initialization failures, read-only state access, and standalone commands.
