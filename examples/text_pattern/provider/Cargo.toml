[package]
name = "geam-example-text-pattern"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
description = "Documentation-oriented regex provider for Geam"
license = "Apache-2.0"
repository = "https://github.com/panarch/geam"
readme = "README.md"
publish = ["crates-io"]
keywords = ["geam", "gleam", "regex", "provider"]
categories = ["development-tools", "text-processing"]

[package.metadata.geam.provider]
schema = 1
gleam-package = "example_text_pattern"
gleam-version = ">= 1.0.0 and < 2.0.0"

[dependencies]
ecow = "=0.2.6"
geam = "=0.2.0"
regex = "1.12"

[workspace]
resolver = "3"
